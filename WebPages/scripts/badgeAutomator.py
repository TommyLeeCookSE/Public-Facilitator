
def process(data):
    split = [line.upper().split('\t') for line in data.split('\n')]

    #Split is a list with lists inside it
    # I want to iterate over split and for every item inside I want to print it on a new line
    #I would call an outerloop to iterate over every item in the list and print it

            
    


#Function that takes in a string, uppercases it returns it
#Fumnction that checks company names, if matches dict, return the dicts key
#Function that takes costCenter and returns without the numbers
    return split